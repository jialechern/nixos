// env.kdl
// 此处配置 niri 桌面环境中必要的环境变量
// 注意: 这里的变量只作用于 niri 启动的子进程 (niri 源码 src/utils/spawning.rs 中
// 以 per-command env 的方式注入), 不会进入 systemd/D-Bus。
// 因此输入法变量 (XMODIFIERS / GTK_IM_MODULE / QT_IM_MODULE / SDL_IM_MODULE /
// GLFW_IM_MODULE) 不在这里配置, 统一由 modules/input-method_and_font.nix 的
// environment.sessionVariables 注入; LANG 也已由系统 i18n.extraLocaleSettings 设置。
environment {
    // 与系统默认不同: 系统 LC_MESSAGES=en_US.UTF-8 (保持命令行工具输出英文),
    // 这里让 niri 启动的图形程序显示中文消息
    LC_MESSAGES "zh_CN.UTF-8"
}
