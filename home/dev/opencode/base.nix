{
  "\$schema" = "https://opencode.ai/config.json";
  # 是否开启自动共享
  autoshare = false;
  # 是否自动检查更新
  autoupdate = false;
  # # 默认使用的模型 (已注释, 保留备用)
  # model = "deepseek/deepseek-v4-flash";
  # 默认使用的模式
  default_agent = "plan";
  # 使用的主题
  theme = "catppuccin-macchiato";
}
