// env.kdl
// 这里的变量只作用于 niri 启动的子进程, 不会进入 systemd/D-Bus;
// 输入法变量与 LANG 由系统层负责 (见 modules/input-method_and_font.nix)
environment {
    // 让 niri 启动的图形程序显示中文消息 (系统默认是 en_US, 便于命令行工具)
    LC_MESSAGES "zh_CN.UTF-8"
}
